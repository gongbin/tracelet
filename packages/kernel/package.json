{
  "name": "@tracelet/kernel",
  "version": "0.1.0",
  "description": "无头 EDA 内核：数据模型、命令系统、连通性、ERC/DRC、导入导出",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "fflate": "^0.8.3",
    "polygon-clipping": "^0.15.7",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@tracespace/parser": "5.0.0-next.0",
    "@types/node": "^22.20.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
