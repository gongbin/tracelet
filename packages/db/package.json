{
  "name": "@tracelet/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PostgreSQL schema（Drizzle）。本地无 Postgres 时不参与构建。",
  "main": "./src/schema.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"(db) no tests\""
  },
  "dependencies": {
    "drizzle-orm": "^0.36.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
