{
  "name": "@tracelet/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "tracelet": "./src/index.ts"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"(cli) no tests yet\""
  },
  "dependencies": {
    "@tracelet/kernel": "workspace:*",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
